# Simpler Search for Linux

This is a wrapper for the find command in Linux, using a more concise and user friendly syntax for common search tasks.


###### Usage
```sh
search filename [path]
```

First parameter is the search term and is mandatory.

The second parameter is the path to search and is optional. The path defaults to current folder (`.`). 
